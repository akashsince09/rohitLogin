<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://java.sun.com/xml/ns/javaee"
         xsi:sergerg

    <display-name>Account Registration Web Application</display-name>

    <!-- Location of Java @Configuration classes that configure the components that makeup this application -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/appconfig-root.xml</param-value>
    </context-param>
ergaerbhaefbhaerhd
    <filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>bhfb fh earh aerh ear earh ear her
        <filter-name>springSecurityFilterChain</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
h erh erh er her
    <servlet>
        <servlet-name>dispatcher</servlet-name>
h        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    h s      <param-name>contextConfigLocation</param-name>
            <param-value></param-value>
 h  he herh       </init-param>
     arherh  erservlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>a h er herhe  herh erh eh
